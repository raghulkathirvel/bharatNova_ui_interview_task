import '../../../utils/core_export.dart';

class SubscritionScreen extends StatelessWidget {
  const SubscritionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Subscribe Screen"),
      ),
    );
  }
}
